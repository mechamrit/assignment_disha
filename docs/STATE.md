Milestone: M7 done (M0 to M7 complete)        Done: M0 fe54682, M0 hardening bbeaff9, M2 a2eeafb, M1 eb6a12f, M3 740fc61, M4 a3e2a03, M5 57418ef, M6 7d61df4, M7 in this session (`git log --oneline`). Pushed to git@github.com:mechamrit/assignment_disha.git (main).
Now: The game survives a player who does not follow the script. Talking over the read-out either scores as a full answer or reads the round again; commands mid read-out are answered and the sequence repeats; a read-out that never reports finishing opens the answer window anyway; cutting the host off keeps what it owed and waits three seconds for the player; silence gets one nudge then a TIMEOUT; an answer that cannot reach the API is retried and then replayed rather than guessed; disconnect and idle end the session at the API. web is still the M0 shell.
Next action: M8. The web UI: features/*, the generated API types, and the RTVI game_state type. Decide the browser client question below first; it is the only thing in the way.
Blocked: one decision, and one thing that needs a key.
  1. The browser voice client: @pipecat-ai/small-webrtc-transport pulls @daily-co/daily-js, which needs Node 22.14 from 0.89.0 on, so on Node 20 only the older set installs (client-js 1.6.x with transport 1.9.0) and its RTVI protocol may not match Python pipecat 1.9.0. Decide at the start of M8: move the repo to Node 22, pin the older client set, or drop engine-strict.
  2. A spoken round needs DEEPGRAM_API_KEY in voice-bot/.env. Everything below the microphone is covered by tests, but no real audio has passed through yet, so the M7 acceptance criteria that need a voice (a "hmm" ignored during a read-out, "wait wait" interrupting inside 300 ms) are proven by the phase tests rather than by ear.
Last verified:
- make ci: exit 0 (api 83 unit tests, web vitest, bot 127 pytest, api and web builds, lint and formatting across all three)
- the phase table against a fake API (20 tests). The M7 rows: a turn that is exactly the sequence is scored with answeredDuringPresentation, any other turn over the read-out sends repeat INTERRUPTED and scores nothing; asking for the score mid read-out answers and re-reads; the presentation watchdog records BOT_ERROR and still opens the answer window; an interrupted host keeps its pending action and waits, and carries on by itself when nothing follows; one nudge then a TIMEOUT; an unreachable API keeps attempt 1, says it lost its notes, and replays the round; a speculative context frame is dropped
- the host tools: each records an intent on the gate and returns run_llm false, so a tool call takes the same path a spoken command does
- the provider switch: gemini, groq, and openai each build their service, an unknown provider raises UnknownProviderError, and an empty provider key falls back to the scripted host
- presentation: the sequence is queued as one TTSSpeakFrame between sentinels with append_to_context false, and the tracker reports started, finished, and interrupted in the right order
- api e2e (3 suites, 23 tests) and the M3 concurrency proofs still pass unchanged
Notes:
- What happens after the host speaks is driven by on_assistant_turn_stopped, not by BotStoppedSpeakingFrame: only the assistant turn says whether the host finished or was cut off, and the two cases need opposite behaviour.
- A retry never changes the attempt number, because the idempotency key the API deduplicates on is built from it. An answer with no verdict is worth zero, so the round is read again instead of guessed at.
- Tools are offered to the model only when the host is a model; the scripted host gets a context with no tools, so it cannot be asked to call one.
- The RTVI observer forwards no bot text, TTS text, or LLM output to the browser, which is what keeps a round's words on the audio path only.
- The gate never awaits the network inside process_frame: it snapshots the turn, flips the phase, and queues a job for a task created on StartFrame, because Pipecat cancels the frame task on an interruption.
- One TTSSpeakFrame per sequence, never one per word: per-word frames would let the bot stop speaking between words, which drops the interruption threshold to a single word.
- The API refuses to score an answer with no vocabulary words (VALIDATION) and records a CHATTER event instead.
- A fresh clone needs `npm run db:generate -w api` before typecheck, tests, or build.
- Prisma is pinned to 6.19.3 and nestjs-pino to 4.6.1 with pino 9, because their newest releases require Node 22.
- Root package.json overrides fastify to 5.12.4 (GHSA-w2qp-rph6-63g4, GHSA-3m5p-2c4r-xxw2, fixed in 5.12.1).
- The allow list in .claude/settings.json is narrower than the sketch in docs/PLAN.md: `curl -s http://localhost*` and `uv run *` both match commands that reach other hosts or run any program.
- memory_bot/api/models.py is still to generate: the committed contracts/openapi.json hides the internal routes on purpose, so a second complete document (contracts/openapi-internal.json) has to be emitted from the same Nest app and generated from, keeping /docs public.
Env: Node 20 via nvm (`nvm use`). Docker via Colima. On this machine the root .env sets REDIS_HOST_PORT=6380 because Homebrew redis owns 6379, and api/.env points DATABASE_URL and REDIS_URL at 127.0.0.1:5432 and 127.0.0.1:6380 (see CLAUDE.local.md). voice-bot/.env does not exist yet; create it from .env.example and add DEEPGRAM_API_KEY for a spoken round.
